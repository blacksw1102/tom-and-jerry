package game.server.main;

public class GameServer implements Runnable {
	
	@Override
	public void run() {
		while(true) {
			// 클라이언트로부터 GameProtocol를 전달받음
			// if GameProtocol == GAME_START면 
			// 
		}
	}
}
